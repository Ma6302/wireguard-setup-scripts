# wg.sh 迭代日志（CHANGELOG）

**对象**：阿里云轻量服务器（`<SERVER_IP>`）WireGuard 安装脚本 —— 第三方汉化改版 `wg.sh`
**现行版本**：**C7**（2009 行 / 75267 字节 / md5 `8e4c316e8f076a2a082daeefc139573c`）—— 脚本内自带版本标记 `WG_SH_VERSION="1.4.0"`
**权威副本**：本地 `..\wg-fixed.sh` ｜ 服务器 `/root/wg.sh` **仍是 C6**（本次按要求**不手工部署**，改由 wgmon 的自动更新在夜间替换，见 C7 一节）
**开源仓库**：https://github.com/Ma6302/wireguard-setup-scripts （2026-09-18 建仓公开；仓库内改名 `wg.sh`，`docs\versions\` 含全部快照，仓库根另有 `WGSH_VERSION` = wg.sh 版本声明文件；文档中服务器 IP 已脱敏为 `<SERVER_IP>`）

> **维护规则**：每次改脚本都要做三件事 —— ① 在 `versions\` 存入新版文件；② 在下表追加一行；③ 在「变更详情」补一节。三处都做完才算一次完整迭代。

---

## 一、版本总览

| 序 | 版本 | 日期 | 行数 | 字节 | md5 | 已部署服务器 | 本目录文件 |
|---|---|---|---|---|---|---|---|
| 00 | **A 原版**（含致命 bug） | 2026-03-27 | 1730 | 58166 | `6545fd467d70c627ed24e3f69756ca93` | ❌ | `versions\00-A-原版-含致命bug-1730行-6545fd46.sh` |
| 01 | **B** AI 改后 | 2026-09-16 | 1730 | 58139 | `0798068d13c1fc2de2e9d07dfa7f9865` | ❌ | `versions\01-B-AI改后-1730行-0798068d.sh` |
| 02 | **S** 服务器旧版 | 2026-09-17 | 1729 | 58138 | `7c78121a438f9645ca5af51eecb89870` | ✅（旧） | `versions\02-S-服务器旧版-1729行-7c78121a.sh` |
| 03 | **C** 修订版（预 F7） | 2026-09-17 12:29 | 1802 | 63175 | `ec72afd03d3bd3c35118075cf91a6654` | ✅ 12:39 | `versions\03-C-修订版-预F7-1802行-ec72afd0.sh` |
| 04 | **C2**（含 F7，预 F8） | 2026-09-17 14:31 | 1826 | 64593 | `b1d658b0f4aecc5db68f7c8181abdeec` | ✅ 14:32 | `versions\04-C2-含F7-预F8-1826行-b1d658b0.sh` |
| 05 | **C3**（含 F8） | 2026-09-17 | 1835 | 65409 | `efeabd5fb4d51be5f3205316add2bf39` | ✅ 16:28 | `versions\05-C3-含F8-1835行-efeabd5f.sh` |
| 06 | **C4**（含 F9） | 2026-09-18 22:07 | 1902 | 68324 | `7ce737cb429200be5ce1074af4495140` | ✅ 22:13 | `versions\06-C4-含F9-1902行-7ce737cb.sh` |
| 07 | **C5**（含 F10） | 2026-09-18 22:36 | 1936 | 71578 | `ca533ced82dfecf48ca08ba4a6b35480` | ✅ 22:38 | `versions\07-C5-含F10-1936行-ca533ced.sh` |
| 08 | **C6**（C5 + 作者信息） | 2026-09-18 23:18 | 1939 | 71735 | `136649cc6fd4c33011559c3aefab2422` | ✅ 23:19 | `versions\08-C6-含F10加作者信息-现行-1939行-136649cc.sh`（文件名沿用，未改名） |
| 09 | **C7**（含 F11 菜单二级化 + 版本标记，**现行**） | 2026-09-19 19:2x | 2009 | 75267 | `8e4c316e8f076a2a082daeefc139573c` | ⏳ 待 wgmon 自动更新 | `versions\09-C7-菜单二级化-2009行-8e4c316e.sh` |

**修复项覆盖矩阵**

| 版本 | F1~F6（DNS 块 6 项） | F7（放行规则跟随端口） | F8（客户端 `::/0` 门控） | F9（wg 显示 peer 名称） | F10（菜单循环） | F11（菜单二级化） |
|---|---|---|---|---|---|---|
| A / B / S | ❌ | ❌ | ❌ | ❌ | ❌ | ❌ |
| C | ✅ | ❌ | ❌ | ❌ | ❌ | ❌ |
| C2 | ✅ | ✅ | ❌ | ❌ | ❌ | ❌ |
| C3 | ✅ | ✅ | ✅ | ❌ | ❌ | ❌ |
| C4 | ✅ | ✅ | ✅ | ✅ | ❌ | ❌ |
| C5 / C6 | ✅ | ✅ | ✅ | ✅ | ✅ | ❌ |
| **C7** | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |

> 校对本目录任何文件：`md5sum versions\*.sh`，结果应与上表逐行一致。
> 若某份 md5 不符 —— 说明本地被改动过，**先别部署，先查来源**。

---

## 二、变更详情（按时间倒序）

### C7 —— 2026-09-19 19:2x ／ **未手工部署**（改由 wgmon 自动更新替换）

**起因**：① 用户要求把管理菜单里三个平级项（列出客户端 / 删除客户端 / 显示 QR 码）合并为一个「管理已有客户端」入口 —— 原设计这三项进来都要先列一遍客户端，重复且占版面；② 为 wgmon 的**双组件自动更新**提供 wg.sh 的版本判据（原来 wg.sh 没有任何版本标识，无法判断新旧）。

**改动**

| 位置 | 改动 |
|---|---|
| 主菜单 `select_menu_option` | 6 项 → **4 项**：`1) 添加新客户端` / `2) 管理已有客户端` / `3) 卸载WireGuard` / `4) 退出`；校验 `^[1-6]$` → `^[1-4]$` |
| 新增 `select_client_menu_option` | 二级菜单「客户端管理」：进入即打印已有客户端清单（`共 N 个` + 缩进的编号列表；无客户端时显示"无"），随后列 `1) 列出所有已存在的客户端 / 2) 删除指定客户端 / 3) 显示指定客户端的QR码 / 4) 返回上一级` |
| 新增 `manage_clients` | 二级循环：承接原 2/3/4 三项功能；子操作完成或空回车取消都回本菜单，只有选 `4` 才 `return` 回主菜单 |
| 新增 `press_enter_to_client_menu` | 二级菜单的暂停函数（文案「按回车键返回客户端管理菜单…」），与主菜单 `press_enter_to_menu` 区分 |
| `select_client_to` | 取消提示「已取消，返回主菜单。」→「已取消，返回客户端管理菜单。」（现在确实回二级菜单） |
| 主循环 | 原 `2) 3) 4)` 三个分支合并为 `2)` 调用 `manage_clients`；卸载 5→3、退出 6→4 |
| 文件顶部 | 新增版本标记 `WG_SH_VERSION="1.4.0"` —— **格式固定**，wgmon 用 `^WG_SH_VERSION="..."` 读取；仓库根 `WGSH_VERSION` 必须同值 |
| 头部注释 | F10 块里「6) 退出」的表述同步改为「4) 退出」；新增 [F11] 说明块 |

**命令行参数完全不受影响**：`--listclients` / `--removeclient` / `--showclientqr` / `--yes` 等分支一字未动，脚本仍可被非交互调用。

**验证（2026-09-19，服务器上以 `/root/wg.sh.c7test` 测试，不覆盖生产文件）**

- 前置：测试副本 md5 `8e4c316e…` 与本地逐字节一致、LF 换行、远端 `bash -n` 通过
- 管道喂入 **9 项交互测试全过**：① 主菜单直接退出；② 进二级 → `4` 返回上一级 → 退出；③ 二级 `1` 列出客户端 → 回车 → 返回 → 退出；④ 二级 `2` 删除 → 空回车取消 → 返回 → 退出；⑤ 二级 `3` QR → 空回车取消 → 返回 → 退出；⑥ 二级 `3` + 编号 `1` **真实渲染 QR 码** → 回车 → 返回 → 退出；⑦ 主菜单输 `9` 报"选择无效"后正常退出；⑧ 二级输 `9` 报错后逐级退出；⑨ **EOF（stdin 立即关闭）** 正常输出"已退出。"（F10 修过的死循环未回归）
- 另用 **PTY（`script -qec`）** 复跑一次，确认真实终端下的提示语与排版：`选择操作 [1-4]：`、`按回车键返回客户端管理菜单…` 均正常
- 零影响复核：`/root/wg.sh` 仍为 C6（`136649cc…`）、`wg-quick@wg0` / `wg-iptables` active、5 peer 完整；测试副本与测试脚本**用后即删**，生产文件**未部署**

**连带（同日 19:3x）**：wgmon 升 **v1.4.0**，更新检测改为**双组件** —— wgmon 与 wg.sh 各自比对版本，谁有新版本更新谁，通知写明"更新了哪个"。服务器上的 wg.sh 将由该机制在夜间自动替换；前提是服务器上的 wgmon 先自更新到 1.4.0（1.3.1 不认识 wg.sh）。

**连带（同日 19:4x）**：用户要求**删除分发包 `组网工具-修复版.zip`**（包内仍是 C6），已删（69,894,255 字节；本次为直接删除，AI 侧工具环境没有可用回收站通道）。此后**不再维护分发包**；若日后需要，用 `_archive\` 里的任一份 zip 快照作底包 + `_archive\rebuild_dist_zip.py`（需改 `SRC_ZIP`）重建即可 —— 快照内的 `FinalShell SSH安装包.exe` / `WireGuard.msi` 与旧主包逐字节同源。

**回滚**：本版未部署，服务器上无需回滚；若自动更新装上了新版而想退回，用 wgmon 留下的 `/root/wg.sh.bak-<时间戳>` 或第六节原有的 `.bak.preC6.20260918` 覆盖即可。

---

### C6 —— 2026-09-18 23:18 ／ 23:19 部署

**起因**：用户要求修改脚本作者信息（GitHub 网名 `Ma6302`），并随后把项目开源到 GitHub。

**改动（纯标注，零逻辑变更）**

| 位置 | 改动 |
|---|---|
| 头部注释块 | 新增两行：`原作者：包崽同学（二改汉化）`、`优化维护：Ma6302（https://github.com/Ma6302）` |
| 运行横幅（原 L75 echo） | 「作者：包崽同学（二改汉化）」→「原作者：包崽同学（二改汉化）」+ 新增「优化维护：Ma6302（https://github.com/Ma6302）」 |
| 上游版权 | **不动** —— 运行时显示的 林松 / Nyr 版权行是上游 MIT 许可的保留声明，必须保留 |

**部署与验证（2026-09-18 23:19）**：服务器原子切换（备份 `/root/wg.sh.bak.preC6.20260918` = C5）；回归「列客户端 → 回车 → 菜单 → 退出」通过，横幅实显新署名，`wg-quick@wg0` 保持 active。

**连带（2026-09-18 23:2x）**：新建公开仓库 **https://github.com/Ma6302/wireguard-setup-scripts** 并推送 C6。仓库内容：`wg.sh`（= C6）+ `harden-ssh.sh` + `wg-forward-guard.sh/.service` + 脱敏版 `CHANGELOG.md` + `docs\versions\`（8 个快照）+ 新写 `README.md` + MIT `LICENSE`。**脱敏**：服务器 IP 替换为 `<SERVER_IP>`；**未入库**：`aliyun.conf`（真实私钥）、66 MB 分发包、含 IP 的审计报告。已验证：远端 SHA = 本地、文件树 18=18、License 识别为 MIT、8 个 topics 生效。

---

### C5 —— 2026-09-18 22:36 ／ 22:38 部署

**起因**：用户提出「`bash wg.sh` 选择相应操作执行完后会自动退出，想做其他操作还要重新运行。希望只有我主动选择退出时才退出」。

**改动（新增 F10：菜单交互重构）**

| 位置 | 改动 |
|---|---|
| 分支 7（已安装→菜单） | 由「显示一次菜单 → 执行一个操作 → `exit`」改为 **`while true` 主菜单循环**；仅「6) 退出」（或确认卸载完成）才结束脚本 |
| 新增 `press_enter_to_menu()` | 有结果输出的操作（含 QR 码的选项 1/4）完成后暂停「按回车键返回主菜单…」——否则循环重印菜单会把 QR 码顶出屏幕扫不了 |
| `select_menu_option()` | 首行文案由「WireGuard已安装完成。」改为菜单标题分隔线；**read 加 EOF 防护**（原代码 Ctrl+D 会死循环） |
| `enter_client_name()` / `select_client_to()` | 空输入（直接回车）由 `abort_and_exit`（退出脚本）改为**返回码 1 = 取消并返回菜单**；提示语补「直接回车取消并返回菜单」 |
| `check_clients()` / `check_client_conf()` | 新增 `menu` 参数：菜单循环调用时返回码 1 而非 `exit 1`；命令行分支（无参调用）行为不变 |
| `confirm_remove_client()` / `confirm_remove_wg()` | read 遇 EOF 视为 **N**（安全默认：不删不卸），防死循环 |
| 选项 5（卸载） | 选 N 中止 → 返回菜单（原为 `exit 1`）；确认卸载完成后正常退出（WireGuard 已不存在，菜单无意义）并提示「重新运行本脚本可重新安装」 |

**设计原则**：有结果输出 → 按回车返回；取消（无结果）→ 直接返回；破坏性操作确认一律默认 N。

**部署与验证（2026-09-18 22:38，7 项测试全过）**

| 测试 | 结果 |
|---|---|
| 服务器脚本 | `/root/wg.sh` 原子切换为 C5（备份 `/root/wg.sh.bak.preC5.20260918` = C4），md5 与本地一致 |
| T1 选项2列客户端 → 回车 → 菜单 → 选6 | ✅ 菜单正确复现，exit 0 |
| T2 选项1添加 → 空名称 | ✅ 「已取消，返回主菜单。」peer 数仍 5（未误增） |
| T3 选项5卸载 → 选N | ✅ 中止回菜单，`wg-quick@wg0` 仍 active（未误卸） |
| T4 选项3删除 → 空编号 | ✅ 取消回菜单，peer 数仍 5（未误删） |
| T5 stdin=`/dev/null`（EOF） | ✅ 打印「已退出。」正常结束，**无死循环** |
| T6 无效输入 9 | ✅ 重提示后正常退出 |
| T7 一次运行连续两个操作 | ✅ 「总计：5个客户端」输出两次 —— 核心需求达成 |

**连带（2026-09-18 23:05）**：重打分发包 `组网工具-修复版.zip`，包内 `wg.sh` 由 C3 → **C5**（71,578 字节 / 1936 行 / `ca533ced…`）。新包 md5 `057f466da940b579b083f6810f2b9a56`（69,894,211 字节）；其余条目（FinalShell / WireGuard.msi）逐字节未变，`testzip()` 全过。重打前快照归档 `_archive\组网工具-修复版.zip.脚本C3版-20260918`（= `f3d10479…`）。**至此分发包追平现行版。**

---

### C4 —— 2026-09-18 22:07 ／ 22:13 部署

**起因**：用户提出「输入 `wg` 或 `wg show` 后，每个 peer 能显示自己的名称」。
WireGuard 原生输出只有公钥（44 字符 base64），人工比对全靠眼力。

**改动（新增 F9）**

| 位置 | 改动 |
|---|---|
| 新增 `install_wg_show_names()` | 写入 `/etc/profile.d/wg-named.sh`，为登录 shell 定义 `wg()` 包装函数；`chmod 644` |
| `finish_setup()` | 调用 `install_wg_show_names` → 全新安装后即生效 |
| 主流程「分支 7：已安装」 | 同样调用 → **已经在跑的老系统，重跑一次脚本也能补上**（幂等） |
| `remove_pkgs()` | 卸载 WireGuard 时 `rm -f /etc/profile.d/wg-named.sh`，不留残渣 |
| 文件头 | 新增 `[F9]` 说明段 |

**实现原理**：名称本来就存在 —— 脚本 `new_client()` 写配置时会插入 `# BEGIN_PEER <名称>` … `# END_PEER <名称>` 标记。包装函数解析 `wg0.conf` 得到「公钥 → 名称」映射，再用 awk 在 `peer: <公钥>` 行尾附加青色 `[名称]`。

**安全性设计（关键）**
- **仅当 stdout 是终端**（`[ -t 1 ]`）时才附加名称 → 管道 / 重定向 / 脚本调用场景输出**与原版 `wg` 逐字节一致**，不会破坏任何解析；
- 只匹配 `^peer: ` 行 → `dump` / `peers` / `latest-handshakes` 等结构化子命令**天然不受影响**；
- 函数只作用于**交互式登录 shell**，`wg-quick` / systemd / 其它脚本仍调用 `/usr/bin/wg` 本体；
- 名称来自脚本自身写入的注释，**增删客户端时自动跟随**，无需额外维护。

**部署与验证（2026-09-18 22:13）**

| 项 | 结果 |
|---|---|
| 服务器脚本 | `/root/wg.sh` 替换为 C4，md5 与本地一致；旧版备份 `/root/wg.sh.bak.preC4.20260918`（= C3） |
| `/etc/profile.d/wg-named.sh` | 已安装（1401 字节，644） |
| T1 `wg show`（终端） | ✅ peer 带名称 |
| T2 `wg`（无参数，终端） | ✅ 5 个 peer 全部正确：phone / computer / mashuaigecomput / mashuaige / wangzikun |
| T3 `wg show \| cat`（管道） | ✅ 无名称，原样输出 |
| T4 `wg show wg0 peers` | ✅ 不受影响 |
| 隧道状态 | `wg-quick@wg0` active，握手时间戳新鲜 —— **对运行中的 VPN 零影响** |

**备注**：`组网工具-修复版.zip` 内含的仍是 C3；C3→C4 只差 F9（纯交互体验，无功能修复）。需要时用 `..\_archive\rebuild_dist_zip.py` 重打。〔后记：2026-09-18 23:05 随 C5 一并重打，见 C5 小节「连带」〕

---

### C3 —— 2026-09-17 ／ 16:28 部署

**改动（新增 F8）**：`new_client()` 下发客户端配置时，`AllowedIPs` 由**无条件**的
`0.0.0.0/0, ::/0` 改为**门控写法**：

```bash
AllowedIPs = 0.0.0.0/0$(grep -q 'fddd:2c4:2c4:2c4::1' "$WG_CONF" && echo ", ::/0")
```

**为什么必须改**：本机（阿里云轻量）**产品层面永久无法开 IPv6**。隧道里带上 `::/0` 会把设备**全部 IPv6 流量**吸进一条没有 IPv6 出口的隧道后丢弃 → **IPv6 黑洞**；而校园网 IPv6 本来免费直连，白白浪费。

⚠️ **不要用 `$ip6` 变量做判据** —— 它只在首次安装时由 `detect_ipv6()` 赋值，后续菜单 `--addclient` 时为空，会误判。门控与同函数 `Address` / 服务端 `AllowedIPs` 的既有写法保持一致。

**部署**：服务器 `/root/wg.sh` 16:28 替换，备份 `/root/wg.sh.bak.preF8.20260917`（= C2）。
**连带**：16:48 重打分发包 `组网工具-修复版.zip`（md5 `f3d10479a5beac10bee8987955c0c409`），包内 `wg.sh` = C3；旧包归档 `_archive\组网工具-修复版.zip.脚本C版-20260917`。

---

### C2 —— 2026-09-17 14:31 ／ 14:32 部署

**改动（新增 F7）**：顶部 DNS 块第 6 步的放行规则**原为写死的 `--dport 53`**，一旦改用其它端口就漏放行（该规则本身也不持久）。已改为**跟随 `--port`**；拿不到端口时交由安装阶段的 `wg-iptables.service`（其中本就用 `$port` 变量）放行。

⚠️ **该行位于脚本顶部、`parse_args` 尚未执行** → 全局变量 `$port` / `$server_port` **此刻都是空值**，只能预扫 `"$@"` 取端口，不能直接引用。

**部署**：服务器 `/root/wg.sh` 14:32 替换，备份 `/root/wg.sh.bak.preF7.20260917`（= C）。

---

### C —— 2026-09-17 12:29 ／ 12:39 部署

**改动（F1~F6，六项）** —— 全部围绕脚本顶部会误伤 DNS 的代码块：

| 编号 | 位置 | 问题 | 修复 |
|---|---|---|---|
| **F1** | 顶部 DNS 块 | 第 8 行 `>/etc/systemd/resolved.conf` **无条件执行**（在任何 `if` 之前），每次运行都把该文件截断为 0 字节；而回填逻辑被 `./wg.txt` 门控，第二次运行不进入 → **配置被永久清空且无人回填**。已在真实服务器复现两次（`resolved.conf` 与 `.bak` 均 0 字节） | 改为在 `wg.txt==1` 门控内做真实备份（`cp -a`，不覆盖已有备份）+ 用 `>` **幂等写入**（原版 `>>` 重复执行会累加） |
| **F2** | `check_args` | DNS 兜底值写成 `233.5.5.5`（**不是有效地址**，应为 `223.5.5.5`）；可达性探测用 `ping google.com`，**依赖 DNS 本身** | 修正地址；探测改为纯 IP `1.1.1.1`，避免「DNS 故障」被误判成「无外网」 |
| **F3** | `select_dns` | 菜单只列 1~4，但 `case` 只匹配 1/2/3/7。选「4) 自定义 DNS」时**没有任何分支命中**，`dns` 停在字符串 `"4"` → 生成非法配置 `DNS = 4`；真正能自定义的是 7，菜单里却没有 | `case` 补 4（保留 7 兼容旧编号）；校验由 `^[1-7]$` 改为 `^[1-4]$`；函数末尾兜底，非法值一律回退 `223.5.5.5` |
| **F4** | `select_dns` 自动模式 | 直接 `dns=2`，覆盖 `check_args` 按外网可达性算出的结果 | 改为仅在当前取值非法时才兜底 |
| **F5** | 顶部 DNS 块 | 为 `/etc/resolv.conf` 建软链前**未确认系统是否使用 systemd-resolved**，在 RHEL/CentOS 系会留下**断链**，直接打坏系统 DNS | 先判断 resolved 是否存在再建链 |
| **F6** | 顶部 DNS 块 | `systemctl stop systemd-resolved` 后**从不重启**，且未说明原因 | 保留等价效果：写入 `DNSStubListener=no` 后 `restart` —— 让解析服务继续可用，同时不占用 53 |

**部署**：服务器 `/root/wg.sh` 12:39 替换为 C 版，备份 `/root/wg.sh.bak.20260917`（= S 版）。

---

### A / B / S（三个基线版本，均**未修复**任何问题）

- **A 原版**（`6545fd46`，1730 行，2026-03-27）：项目最初拿到的那份，**含 F1 致命 bug**。
- **B AI 改后**（`0798068d`，1730 行，2026-09-16）：另一轮 AI 改过的版本，位于 `..\wg-wg执行文件\wg.sh`，**F1 之外的六项一个没修**。
- **S 服务器旧版**（`7c78121a`，1729 行）：故障发生时服务器上实际在跑的那份，从服务器取回留证。

> ⚠️ 这三份**只作留证，永远不要再部署**。

---

## 三、安全部署流程（已验证三次，可复用）

脚本本身不启动任何服务，**替换文件对正在跑的隧道零影响**。全程用 `ssh -i ~/.ssh/workbuddy_aliyun`（AI 侧唯一可用凭据）。

```bash
# ① 备份现行版（带代号与日期）
cp -p /root/wg.sh /root/wg.sh.bak.pre<代号>.<日期>

# ② scp 到临时名（不覆盖）
scp -i ~/.ssh/workbuddy_aliyun wg-fixed.sh root@<SERVER_IP>:/root/wg.sh.new

# ③ 远端核对：md5 / 行数 / 语法 / 关键行位置
ssh -i ~/.ssh/workbuddy_aliyun root@<SERVER_IP> \
  'md5sum /root/wg.sh.new; wc -l /root/wg.sh.new; bash -n /root/wg.sh.new && echo SYNTAX_OK'

# ④ 原子切换
ssh -i ~/.ssh/workbuddy_aliyun root@<SERVER_IP> \
  'mv -f /root/wg.sh.new /root/wg.sh && chmod 755 /root/wg.sh'

# ⑤ 复核服务与隧道
ssh -i ~/.ssh/workbuddy_aliyun root@<SERVER_IP> \
  'systemctl is-active wg-quick@wg0; wg show wg0 latest-handshakes'
```

**验证 F9 这类 profile.d 功能**需要伪终端（普通 ssh 命令的 stdout 是管道，函数会走透传分支）：

```bash
ssh -tt -i ~/.ssh/workbuddy_aliyun root@<SERVER_IP> 'bash -lc "wg show"'
```

---

## 四、回滚

**本地**

```bash
cp -p "wg脚本迭代\versions\06-C4-含F9-1902行-7ce737cb.sh" wg-fixed.sh   # 回到 C4（7ce737cb）
```

**服务器**（按版本逐级回退）

```bash
# 退 C5/F10 → C4
mv /root/wg.sh.bak.preC5.20260918 /root/wg.sh && chmod 755 /root/wg.sh
# 退 C4/F9（同时清掉显示增强）
mv /root/wg.sh.bak.preC4.20260918 /root/wg.sh && chmod 755 /root/wg.sh && rm -f /etc/profile.d/wg-named.sh
# 退 F8 → C2
mv /root/wg.sh.bak.preF8.20260917 /root/wg.sh && chmod 755 /root/wg.sh
# 退 F7 → C
mv /root/wg.sh.bak.preF7.20260917 /root/wg.sh && chmod 755 /root/wg.sh
# 退到 S 版（基线，无任何修复）
mv /root/wg.sh.bak.20260917 /root/wg.sh && chmod 755 /root/wg.sh
```

服务器现存备份：`preC5.20260918`(=C4) / `preC4.20260918`(=C3) / `preF8.20260917`(=C2) / `preF7.20260917`(=C) / `20260917`(=S)。

---

## 五、已知问题与注意事项

1. **调用脚本前先 `cd /root`** —— DNS 块的门控用相对路径 `./wg.txt`，不在 `/root` 下调用会重建该文件为 `1`，导致 DNS 块重新执行、**上游 DNS 被改成 `1.1.1.1`**（本机最优解是阿里云内网 `100.100.2.136` / `100.100.2.138`，别动）。
2. **`$port` / `$server_port` 在脚本顶部是空值** —— 顶部的代码在 `parse_args` 之前执行，引用它们必然踩坑（见 F7）。
3. **不要用 `$ip6` 判断 IPv6 出口** —— 它只在首次安装时赋值（见 F8）。
4. **该机永久无 IPv6**（阿里云轻量明文不支持）→ 不要再设计任何「服务器提供 IPv6 出口」的方案。
5. ~~**分发包 `组网工具-修复版.zip` 内含 C3，比现行版落后一代**~~ —— **已于 2026-09-18 23:05 重打追平**：包内 `wg.sh` = C5（`ca533ced…`），新包 md5 `057f466d…`；C3 版旧包快照在 `_archive\组网工具-修复版.zip.脚本C3版-20260918`。
6. **合规风险**：全隧道 + 服务器做出口，在阿里云《轻量应用服务器使用须知》中属**明文禁止的「流量穿透服务」**。2026-09-17 21:55 平台已**实际动手封过一次**全出方向（对外攻击告警），别跨境、别共享、别跑大流量。

---

## 六、命名约定

```
versions\<两位序号>-<代号>-<关键特征>-<行数>行-<md5前8位>.sh
例：06-C4-含F9-现行-1902行-7ce737cb.sh
```

- 序号两位递增，保证按名称排序即等于按时间排序；
- 文件名内嵌「行数 + md5 前 8 位」，**双击即可肉眼校验**，不必开文件；
- 只有**最新版**带「现行」字样；下一个版本发布时，记得把旧版文件名里的「现行」去掉；
- 每次迭代结束，同步更新：本文件 `CHANGELOG.md` 的「版本总览」+「变更详情」，以及 `..\_archive\README.md`、`..\项目交接说明.md`、`..\.workbuddy\memory\MEMORY.md`。
